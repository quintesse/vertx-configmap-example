name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a multi-line script
      run: |
        SHORTSHA=$(echo "${{ github.sha }}" | cut -c-7)
        echo "SHORTSHA=${SHORTSHA}"
    - name: DAre ENV vars persistent?
      run: |
        echo "SHORTSHA=${SHORTSHA}"
